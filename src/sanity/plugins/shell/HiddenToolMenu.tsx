'use client'

/**
 * Suppresses Studio's default horizontal tool tab strip — tools are
 * rendered instead by `ToolRail` inside the `studio.components.layout`
 * override (see `StudioLayout.tsx`).
 */
export function HiddenToolMenu(): null {
  return null
}
